# web application

```text

traditional web( served) application
    user interface
        HTML documents
            forms
        HTTP redirects

how a web page could be fetched and presented to user
    browser ___
        makes request to fetch HTML that represents web page
        parses the downloaded HTML
        makes additional requests corresponding to execution scripts
        layout information and sub-resources to display
        combines resource to render and present web page
        scripts executed by the browser can make additional requests in later phases
        
```

