# Unix

```text

Unix
    access control
        decisions
            depends on
                user attempting to perform action
                user's membership in Unix group
        users
            root
                omnipotent
                owns every object
                leaves no record of operations performed
            owners
                owns
                    objects
                        they create
    objects
        files
        processes
    sudo
        execute commands as another user
            example
                root
            consults /etc/sudoers
            keeps logs of command line
        
```

